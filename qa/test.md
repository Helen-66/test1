# QA Workflow Rules

## Hard Rules

1. All QA work (modifications and new files) must only occur within the `qa/` directory.
2. Development source code must not be modified during QA.
3. Any bugs or issues discovered during testing must be recorded in `qa/bugs/`.
